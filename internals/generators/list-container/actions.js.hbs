import { create{{ properCase type}}Actions } from 'redux-immutable-crud';
import { moduleName } from './constants';

const defaultActions = create{{ properCase type}}Actions(moduleName);

export default defaultActions;
