import constants from './constants';
import { create{{ properCase type}}Reducer } from 'redux-immutable-crud';
const defaultReducer = create{{ properCase type}}Reducer(constants);

export default defaultReducer;
